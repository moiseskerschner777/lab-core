# Phase 5 — Search Endpoint

**Goal:** `POST http://localhost:8001/search` returns ranked code chunks from IRIS for a natural language query. Results include file, line, type, name, score, and text.

**Flow:** Flow 1 — Build→Observe→Adjust

**References:** `design.md` → `routers/search.py`, API Contracts → POST /search

**Prerequisites:** Phase 4 complete. At least one project indexed.

---

> **When a task is complete, mark it `[-]`.**

- [ ] 5.1 Create `app/routers/search.py`. Implement `POST /search`:

  Request model:
  ```python
  class SearchRequest(BaseModel):
      collection: str
      query: str
      top_k: int = config.SEARCH_TOP_K
  ```

  Handler logic:
  1. `collection_exists(conn, request.collection)` — raise `HTTPException(404, "Collection '...' not found")` if false.
  2. `results = retrieve(request.query, request.collection, request.top_k)`
  3. Return:
  ```python
  {
      "collection": request.collection,
      "query": request.query,
      "results": results
  }
  ```

  **Test:**
  - Do: `docker compose run --rm app python -c "from app.routers.search import router; print(router.routes)"`
  - Expect: one POST /search route. No import errors.

- [ ] 5.2 Commit 5.1.

  **Test:**
  - Run: `git add -A && git commit -m "5.1: search router"`
  - Expect: commit succeeds, clean working tree.

- [ ] 5.3 Register the search router in `app/main.py`.

  ```python
  from app.routers import health, index, collections, search
  app.include_router(search.router)
  ```

  **Test:**
  - Do: `docker compose up --build`
  - Expect: `/docs` shows `/health`, `/index`, `/collections`, `/search`. No errors on startup.

- [ ] 5.4 Commit 5.3.

  **Test:**
  - Run: `git add -A && git commit -m "5.3: register search router"`
  - Expect: commit succeeds, clean working tree.

- [ ] 5.5 Verify 404 on unknown collection.

  **Test:**
  - Do:
    ```
    curl -s -X POST http://localhost:8001/search \
      -H "Content-Type: application/json" \
      -d '{"collection": "does_not_exist", "query": "hello"}' | python -m json.tool
    ```
  - Expect: HTTP 404 with `{"detail": "Collection 'does_not_exist' not found"}`.

- [ ] 5.6 End-to-end search test — query a real indexed project.

  **Test:**
  - Do:
    ```
    curl -s -X POST http://localhost:8001/search \
      -H "Content-Type: application/json" \
      -d '{"collection": "projecta", "query": "database connection", "top_k": 5}' \
      | python -m json.tool
    ```
  - Expect: response with `"results"` array containing 5 items. Each item has `score`, `file`, `type`, `name`, `start_line`, `end_line`, `text`. Top result is visibly related to database/connection code.

- [ ] 5.7 Quality check — three diverse queries.

  **Test:**
  - Do: Run these three searches and read the top 3 results of each:
    ```
    curl -s -X POST http://localhost:8001/search \
      -H "Content-Type: application/json" \
      -d '{"collection": "projecta", "query": "how are errors handled", "top_k": 3}' | python -m json.tool

    curl -s -X POST http://localhost:8001/search \
      -H "Content-Type: application/json" \
      -d '{"collection": "projecta", "query": "authentication and user session", "top_k": 3}' | python -m json.tool

    curl -s -X POST http://localhost:8001/search \
      -H "Content-Type: application/json" \
      -d '{"collection": "projecta", "query": "data models and schema", "top_k": 3}' | python -m json.tool
    ```
  - Expect: for each query, at least 2 of the top 3 results are clearly semantically relevant. Note any obviously wrong results in Phase 6.

- [ ] 5.8 Index all three projects and verify cross-collection isolation.

  **Test:**
  - Do:
    ```
    curl -s -X POST http://localhost:8001/index \
      -H "Content-Type: application/json" \
      -d '{"path": "/workspace/projectB"}' | python -m json.tool

    curl -s -X POST http://localhost:8001/index \
      -H "Content-Type: application/json" \
      -d '{"path": "/workspace/projectC"}' | python -m json.tool

    curl -s http://localhost:8001/collections | python -m json.tool
    ```
  - Expect: both index responses return `"status": "ok"`. Collections response lists `projecta`, `projectb`, `projectc`. A search on `projectb` returns only chunks from `projectB` files.
